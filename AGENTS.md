# AI / agent instructions (Docplanner)

Start here whenever you work in this repo.

1) Read shared project context: `ai-context/_shared/README.md`
2) If you are a tool-specific agent, also read:
   - Codex: `ai-context/codex/README.md`
   - Cursor: `.cursor/rules/00-ai-context.mdc`
   - Claude: `.claude/CLAUDE.md`

When instructions conflict, prefer `_shared` unless a tool-specific file explicitly overrides it.

