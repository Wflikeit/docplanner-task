# Codex-specific notes

Codex entrypoint is `AGENTS.md` in the repo root. Always read:
- `ai-context/_shared/README.md`
- `ai-context/task-details.md`

Operational preferences:
- Prefer `rg` for searching.
- When you touch files, keep changes minimal and run the narrowest relevant checks (lint/build) if available.
- When adding new AI-context docs, keep them in `ai-context/_shared/` and link from here.

