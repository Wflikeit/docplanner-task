/**
 * Otodom crawl adapter: detail merge only (raw capture). Snapshot + LD types live in
 * `data-pipeline/otodom/raw/` (re-exported here for stable `crawler/otodom` imports).
 */
export * from './listingSnapshot.js'
export * from './ldJsonHouse.js'
export * from './detailMerge.js'
